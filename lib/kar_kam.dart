// Import flutter packages.
import 'package:flutter/material.dart';

class KarKam extends StatelessWidget {
  const KarKam({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
